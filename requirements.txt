# Core dependencies
# 注意：TensorFlow 2.10 仅支持 Python 3.9-3.10
# 在 Python 3.11+ 环境下，TensorFlow >= 2.12
# 本项目目标环境为 Python 3.9.13 + TensorFlow 2.10.1
# 当前requirements.txt为简化依赖，适用于开发测试
# 生产环境请使用 Poetry 管理精确版本
pandas>=1.5.0
numpy>=1.23.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.28.0
pyarrow>=10.0.0
loguru>=0.6.0
tushare>=1.2.89

# Development dependencies
pytest>=7.2.0
pytest-cov>=4.0.0
black>=22.10.0
flake8>=5.0.0
isort>=5.10.0
