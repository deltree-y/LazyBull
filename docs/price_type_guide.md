# 回测价格口径说明与迁移指南

## 目录
- [价格口径概述](#价格口径概述)
- [当前实现](#当前实现)
- [推荐实践](#推荐实践)
- [迁移指南](#迁移指南)
- [常见问题](#常见问题)

---

## 价格口径概述

### 三种价格类型

在A股回测中，常见三种价格口径：

#### 1. 不复权价格 (close)

**定义：** 股票的实际交易价格，未经任何调整。

**特点：**
- 反映真实市场价格
- 用于计算实际交易成本
- 分红、配股后会产生价格跳空

**适用场景：**
- ✅ **计算交易成本**（佣金、印花税、滑点）
- ✅ **模拟实际下单价格**
- ✅ **计算实际持仓市值**

**示例：**
```
日期         价格
2023-01-01  10.00
2023-01-02  10.50  (上涨5%)
2023-01-03  5.25   (10送10，价格减半，但持仓股数翻倍)
```

#### 2. 后复权价格 (close_adj / 后复权)

**定义：** 以上市日价格为基准，向后调整当前价格。

**特点：**
- 消除分红、配股的影响
- 价格连续，无跳空
- 可以直观看到真实涨跌幅

**适用场景：**
- ✅ **计算收益率**（训练标签）
- ✅ **技术分析**（均线、MACD等）
- ✅ **绩效评估**

**示例：**
```
日期         不复权价格  后复权价格
2023-01-01  10.00      20.00
2023-01-02  10.50      21.00  (上涨5%)
2023-01-03  5.25       21.00  (10送10后调整，持有收益不变)
```

#### 3. 前复权价格 (close_qfq / 前复权)

**定义：** 以当前价格为基准，向前调整历史价格。

**特点：**
- 保持当前的基准
- 历史价格可能很低甚至出现负数
- 较少使用

**适用场景：**
- 📊 查看历史涨幅
- 📊 长期持有收益分析

---

## 当前实现

### 项目当前使用的价格

LazyBull 项目当前实现：

1. **特征构建与标签计算**
   - 使用：**后复权价格 (close_adj)**
   - 位置：`src/lazybull/features/builder.py`
   - 用途：计算未来N日收益率标签

2. **回测成本计算**
   - **改进前**：使用 `close` 列（可能是复权或不复权，取决于数据源）
   - **改进后**：支持配置，默认推荐使用 `close`（不复权）
   - 位置：`src/lazybull/backtest/engine.py`

### 问题诊断

**潜在问题：**

如果回测时使用了复权价格计算成本，会导致：

1. **成本计算不准确**
   - 实际交易按不复权价格成交
   - 使用复权价格会高估或低估成本

2. **回测结果失真**
   - 净值曲线可能不准确
   - 收益率计算偏差

**示例：**

```
假设：
- 股票 A 在 2023-01-01 价格 10元
- 2023-01-02 进行 10送10 分红
- 2023-01-03 价格 5元

不复权价格：
- 2023-01-01: 10元
- 2023-01-03: 5元
- 买入成本 = 10 * 1000 * (1 + 0.0003) = 10003元

后复权价格：
- 2023-01-01: 10元 (复权后)
- 2023-01-03: 10元 (复权后)
- 如果用复权价格计算成本：
  买入成本 = 20 * 1000 * (1 + 0.0003) = 20006元 ❌ 错误！
```

---

## 推荐实践

### 最佳实践

根据业界通用做法和项目实际情况，推荐：

| 场景 | 推荐价格类型 | 原因 |
|------|--------------|------|
| **交易成本计算** | **不复权价格 (close)** | 反映真实交易价格，成本计算准确 |
| **收益率计算** | **后复权价格 (close_adj)** | 消除分红影响，收益率连续 |
| **技术指标** | **后复权价格 (close_adj)** | 价格连续，指标计算准确 |
| **持仓市值** | **不复权价格 (close)** | 反映真实市值 |
| **绩效评估** | **后复权价格 (close_adj)** | 考虑分红再投资收益 |

### 配置示例

#### 方式一：代码配置（推荐）

```python
from src.lazybull.backtest import BacktestEngine
from src.lazybull.common.cost import CostModel

# 创建回测引擎（使用不复权价格计算成本）
engine = BacktestEngine(
    universe=universe,
    signal=signal,
    initial_capital=1000000,
    cost_model=CostModel(),
    rebalance_freq="W",
    price_type='close',  # 使用不复权价格（默认，推荐）
    verbose=False
)

# 如果需要使用复权价格（不推荐用于成本计算）
engine_adj = BacktestEngine(
    universe=universe,
    signal=signal,
    price_type='close_adj',  # 使用后复权价格
    verbose=False
)
```

#### 方式二：配置文件

在 `configs/base.yaml` 中添加：

```yaml
backtest:
  initial_capital: 1000000
  rebalance_frequency: "M"
  price_type: "close"  # 不复权价格（推荐）
  verbose: true
```

### 数据准备

确保价格数据包含所需的列：

```python
# 加载日线数据（clean 层已包含复权价格）
daily_data = loader.load_clean_daily(start_date, end_date)

# 检查必需的列
required_columns = ['ts_code', 'trade_date', 'close', 'close_adj']
assert all(col in daily_data.columns for col in required_columns)

# 数据示例
print(daily_data.head())
#    ts_code   trade_date   close  close_adj  open  high  low
# 0  000001.SZ  20230103    10.50    21.00    10.30 10.60 10.20
# 1  000002.SZ  20230103    15.20    18.50    15.00 15.30 14.90
```

---

## 迁移指南

### 迁移步骤

如果您之前使用了复权价格进行回测，建议按以下步骤迁移：

#### 步骤 1：检查当前配置

```python
# 检查回测引擎的价格类型设置
print(f"当前价格类型: {engine.price_type}")

# 检查数据是否包含不复权价格
print(f"数据列: {daily_data.columns.tolist()}")
```

#### 步骤 2：更新回测配置

```python
# 旧代码（可能使用复权价格）
engine_old = BacktestEngine(
    universe=universe,
    signal=signal,
    # 未指定 price_type，默认使用 close 列
)

# 新代码（明确使用不复权价格）
engine_new = BacktestEngine(
    universe=universe,
    signal=signal,
    price_type='close',  # 明确指定不复权价格
    verbose=False
)
```

#### 步骤 3：重新运行回测

```python
# 使用新配置重新运行回测
nav_curve_new = engine_new.run(
    start_date=pd.Timestamp('2023-01-01'),
    end_date=pd.Timestamp('2023-12-31'),
    trading_dates=trading_dates,
    price_data=daily_data
)

# 生成新的回测报告
from src.lazybull.backtest import Reporter
reporter = Reporter()
trades = engine_new.get_trades()
stats = reporter.generate_report(nav_curve_new, trades)
```

#### 步骤 4：对比结果

```python
# 对比旧版本和新版本的差异
def compare_backtest_results(old_stats, new_stats):
    """
    对比两次回测的差异
    """
    metrics = ['total_return', 'annual_return', 'max_drawdown', 'sharpe_ratio']
    
    print("=" * 60)
    print("回测结果对比（旧版 vs 新版）")
    print("=" * 60)
    
    for metric in metrics:
        old_val = old_stats.get(metric, 0)
        new_val = new_stats.get(metric, 0)
        diff = new_val - old_val
        diff_pct = (diff / old_val * 100) if old_val != 0 else 0
        
        print(f"{metric}:")
        print(f"  旧版: {old_val:.4f}")
        print(f"  新版: {new_val:.4f}")
        print(f"  差异: {diff:.4f} ({diff_pct:+.2f}%)")
        print()

compare_backtest_results(old_stats, new_stats)
```

### 预期差异

使用不复权价格后，预期差异：

1. **交易成本**
   - 差异幅度：±0.1% - 0.5%
   - 取决于回测期间的分红、配股频率

2. **净值曲线**
   - 形状基本一致
   - 绝对值可能有微小差异

3. **收益率**
   - 差异通常 < 1%
   - 如果差异 > 2%，需要检查数据质量

### 差异处理

如果发现显著差异（> 2%）：

1. **检查数据质量**
   ```python
   # 检查是否有异常的复权因子
   adj_factor_check = daily_data.groupby('ts_code')['adj_factor'].agg(['min', 'max', 'std'])
   print(adj_factor_check.describe())
   ```

2. **检查特殊日期**
   ```python
   # 查找分红配股日期
   large_adj_changes = daily_data[
       daily_data.groupby('ts_code')['adj_factor'].diff().abs() > 0.1
   ]
   print(large_adj_changes)
   ```

3. **验证成本计算**
   ```python
   # 抽查几笔交易的成本计算
   sample_trades = trades.sample(10)
   for _, trade in sample_trades.iterrows():
       print(f"股票: {trade['stock']}, "
             f"价格: {trade['price']}, "
             f"成本: {trade['cost']}")
   ```

---

## 常见问题

### Q1: 为什么推荐使用不复权价格计算成本？

**A:** 因为实际交易时，交易所按不复权价格成交。使用不复权价格可以：
- 准确计算实际成本（佣金、印花税、滑点）
- 准确计算实际持仓市值
- 回测结果更接近实盘

### Q2: 那收益率为什么要用复权价格？

**A:** 收益率应该考虑分红再投资的收益。使用复权价格可以：
- 消除分红、配股导致的价格跳空
- 计算真实的持有收益率
- 与基准指数（通常是全收益指数）可比

### Q3: 如果数据只有复权价格怎么办？

**A:** 可以通过复权因子反推不复权价格：

```python
# 从后复权价格反推不复权价格
df['close'] = df['close_adj'] / df['adj_factor']

# 验证
print(df[['close', 'close_adj', 'adj_factor']].head())
```

### Q4: 前复权和后复权有什么区别？

**A:** 主要是基准不同：

- **前复权 (close_qfq)**: 以当前价格为基准，调整历史价格
  - 优点：当前价格就是市场价格
  - 缺点：历史价格会随着分红变化

- **后复权 (close_adj)**: 以上市价为基准，调整当前价格
  - 优点：历史价格固定不变
  - 缺点：当前价格可能很高，不直观

LazyBull 项目使用**后复权（后复权价格，close_adj）**，这是业界主流做法。

### Q5: 如何验证价格口径是否正确？

**A:** 可以通过以下方式验证：

```python
def validate_price_types(df, stock_code='000001.SZ', date='20230103'):
    """
    验证不同价格口径
    """
    stock_data = df[
        (df['ts_code'] == stock_code) &
        (df['trade_date'] == date)
    ]
    
    if len(stock_data) == 0:
        print(f"未找到 {stock_code} 在 {date} 的数据")
        return
    
    row = stock_data.iloc[0]
    
    print(f"股票: {stock_code}, 日期: {date}")
    print(f"不复权价格 (close): {row['close']:.2f}")
    print(f"后复权价格 (close_adj): {row['close_adj']:.2f}")
    print(f"复权因子 (adj_factor): {row['adj_factor']:.4f}")
    print(f"验证: close * adj_factor = {row['close'] * row['adj_factor']:.2f}")
    print(f"      应该等于 close_adj = {row['close_adj']:.2f}")
    
    # 应该相等（允许小误差）
    assert abs(row['close'] * row['adj_factor'] - row['close_adj']) < 0.01

validate_price_types(daily_data)
```

### Q6: 迁移后回测结果变化很大，是否正常？

**A:** 通常变化应该很小（< 1%）。如果变化很大，可能原因：

1. **数据问题**
   - 检查复权因子是否正确
   - 检查是否有缺失数据

2. **配置问题**
   - 确认之前使用的价格类型
   - 确认成本模型参数未变

3. **期间有大量分红**
   - 查看回测期间的分红情况
   - 大量分红会导致不复权和复权价格差异较大

建议：先在小范围（如1个月）进行测试，验证差异在预期范围内，再扩展到完整回测期。

---

## 总结

### 核心原则

1. **成本计算用不复权，收益计算用复权**
2. **数据层面区分清楚，配置层面灵活选择**
3. **迁移时做好对比验证**

### 项目当前状态

- ✅ 特征构建：使用后复权价格（正确）
- ✅ 成本计算：支持配置，推荐不复权价格（已改进）
- ✅ 文档完善：价格口径说明清晰
- ✅ 向后兼容：保持原有 API 不变

### 后续建议

1. **在 README 中增加价格口径说明**
2. **在回测报告中注明使用的价格类型**
3. **建立数据质量检查流程**
4. **定期验证复权因子的准确性**

---

**注意：** 如果对价格口径仍有疑问，建议先阅读 TuShare 的[复权价格说明](https://tushare.pro/document/2?doc_id=146)。
